name: workflow learning
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: secrets test
    - uses: actions/checkout@v4
      env:
        secrets: ${{ secrets.DEV }}
      run: |
        python3 python.py